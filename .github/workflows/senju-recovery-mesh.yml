name: Senju Recovery Mesh

on:
  push:
    paths:
      - 'senju/senju/recovery_mesh.py'
      - 'senju/senju/emergency_stop_state.py'
      - 'senju/senju/emergency_recovery.py'
      - 'senju/senju/guard_timeout.py'
      - 'senju/tests/test_recovery_mesh.py'
      - 'senju/tests/test_emergency_stop_state.py'
      - 'senju/tests/test_emergency_recovery.py'
      - 'senju/tests/test_guard_timeout.py'
      - 'senju/RECOVERY_MESH.md'
      - 'senju/pyproject.toml'
      - '.github/workflows/senju-recovery-mesh.yml'
  pull_request:
    paths:
      - 'senju/senju/recovery_mesh.py'
      - 'senju/senju/emergency_stop_state.py'
      - 'senju/senju/emergency_recovery.py'
      - 'senju/senju/guard_timeout.py'
      - 'senju/tests/test_recovery_mesh.py'
      - 'senju/tests/test_emergency_stop_state.py'
      - 'senju/tests/test_emergency_recovery.py'
      - 'senju/tests/test_guard_timeout.py'
      - 'senju/RECOVERY_MESH.md'
      - 'senju/pyproject.toml'
      - '.github/workflows/senju-recovery-mesh.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  recovery-mesh-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'

      - name: Install Senju and test dependency
        run: python -m pip install --disable-pip-version-check -e './senju[dev]'

      - name: Run Recovery Mesh, Emergency Stop, and Guard Timeout tests
        run: pytest -q senju/tests/test_recovery_mesh.py senju/tests/test_emergency_stop_state.py senju/tests/test_emergency_recovery.py senju/tests/test_guard_timeout.py

      - name: Compile Recovery Mesh, Emergency Stop, and Guard Timeout modules
        run: python -m py_compile senju/senju/recovery_mesh.py senju/senju/emergency_stop_state.py senju/senju/emergency_recovery.py senju/senju/guard_timeout.py
