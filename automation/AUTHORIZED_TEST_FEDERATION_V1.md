# AUTHORIZED TEST FEDERATION V1

Use `/AUTHORIZED_TEST_TARGETS.json` as the canonical registry and `/senju/config/authorized-test-federation-broadcast.json` as the fleet directive. Actively assess verified federation members at normal rates. External links become in-scope only after membership verification; ordinary third-party links do not inherit authorization.
