# Authorized Test Target

Canonical registry: `../AUTHORIZED_TEST_TARGETS.json`

The exact HTTPS host `kabeya-authorized-test-range.onrender.com` is explicitly authorized. Every same-origin path, query, fragment, and internal/relative URL inherits authorization recursively. Different hostnames never inherit authorization from a link.
