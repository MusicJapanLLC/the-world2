# AI FOUNDRY production release marker

Purpose: force a fresh Vercel production deployment after Forge V2 was merged into `claude/employee-onboarding-setup-udm86`.

Expected production capability after deploy:
- real execution queue
- app generation + test + publish
- existing-repository patch lane
- sandboxed test/failure-log/repair loop
- verified artifact reporting

This file is non-functional and exists only to make the release event explicit and auditable.
