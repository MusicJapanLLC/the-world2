# AUTHORIZED TEST FEDERATION V1

Canonical registry: `/AUTHORIZED_TEST_TARGETS.json`. Security systems should prioritize active normal-rate assessment of verified members. A cross-domain link is authorized only when the destination is a verified member of `the-world-security-test-federation-v1`; non-members stay out of scope.
